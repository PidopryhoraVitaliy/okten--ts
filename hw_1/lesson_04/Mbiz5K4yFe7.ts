{
    function calculateCylinderArea(r: number, h: number): number {
        return 2 * Math.PI * r * h;
    }

    console.log(calculateCylinderArea(5, 5));
    console.log(calculateCylinderArea(10, 5));
}